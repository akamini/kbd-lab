# 投稿機能の実装について

## 概要

ログインしたユーザーが自作キーボード、キーキャップ、小物、工具などのプロダクトを投稿できる機能を実装しました。

## 実装した機能

### 1. 投稿ページ (`/post`)

- ログインしたユーザーのみアクセス可能
- 未ログインユーザーは自動的にホームページにリダイレクトされ、ログインモーダルが表示される

### 2. 投稿フォーム

以下の項目を入力できます：

- **タイトル** (必須)
- **説明** (必須)
- **カテゴリ** (必須): キーボード、キーキャップ、小物、工具から選択
- **GitHub URL** (任意): プロジェクトの GitHub リポジトリ
- **ショップ URL** (任意): 販売ページの URL
- **タグ** (任意): 既存タグから選択、または新規タグを作成
- **画像** (任意): 最大 5 枚まで画像をアップロード可能

### 3. 画像アップロード機能

- Supabase Storage を使用
- 最大 5 枚まで画像をアップロード可能
- 画像プレビュー機能
- 画像の削除機能
- ユーザーごとのフォルダに自動分類

### 4. タグ機能

- 既存タグから選択
- 新しいタグの作成
- 複数タグの選択
- タグの削除

### 5. ヘッダーの更新

- ログインユーザーには「投稿」ボタンを表示
- 投稿ページへの直接アクセス

## データベース設計

### テーブル構成

1. **profiles**: ユーザープロフィール情報
2. **categories**: プロダクトカテゴリ
3. **tags**: タグ情報
4. **products**: プロダクト情報
5. **product_tags**: プロダクトとタグの中間テーブル
6. **favorites**: お気に入り機能（将来実装予定）

### RLS (Row Level Security) ポリシー

- **プロダクト**: 誰でも閲覧可能、作成者のみ編集・削除可能
- **タグ**: 誰でも閲覧可能、認証済みユーザーは作成可能、管理者のみ編集・削除可能
- **カテゴリ**: 誰でも閲覧可能、管理者のみ作成・編集・削除可能
- **画像**: 誰でも閲覧可能、自分のフォルダにのみアップロード・編集・削除可能

## セットアップ手順

### 1. データベースの作成

Supabase のクエリエディタで以下のファイルを実行してください：

```
supabase/create_tables.sql
```

### 2. 環境変数の確認

以下の環境変数が設定されていることを確認してください：

```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 3. 依存関係のインストール

```bash
npm install sonner
```

### 4. UI コンポーネントの追加

```bash
npx shadcn@latest add badge select input label textarea
```

## 使用方法

### 1. ログイン

- ヘッダーの「ログイン」ボタンから GitHub でログイン

### 2. 投稿

- ログイン後、ヘッダーの「投稿」ボタンをクリック
- 投稿フォームに必要な情報を入力
- 「投稿する」ボタンで投稿完了

### 3. 画像アップロード

- 投稿フォームの画像エリアをクリック
- 最大 5 枚まで画像を選択
- プレビューで確認後、投稿

### 4. タグの管理

- 既存タグから選択
- 新しいタグ名を入力して「追加」ボタンで新規作成
- 選択したタグは × ボタンで削除可能

## 技術スタック

- **フロントエンド**: Next.js 15, React 19, TypeScript
- **UI**: shadcn/ui, Tailwind CSS
- **バックエンド**: Supabase (Database, Auth, Storage)
- **通知**: Sonner (Toast notifications)
- **アイコン**: Lucide React

## セキュリティ

- RLS (Row Level Security) による適切なアクセス制御
- 画像アップロードは認証済みユーザーのみ
- ユーザーは自分のプロダクトのみ編集・削除可能
- XSS 対策のための適切なサニタイゼーション

## 今後の拡張予定

- プロダクト一覧ページの実装
- プロダクト詳細ページの実装
- お気に入り機能の実装
- 検索・フィルタリング機能
- プロダクト編集機能
- ユーザープロフィールページ
- 管理者機能
